# جلسه اول
## دلایل استفاده از `github`

## اشتراک گذاری پروژه
ما یک پروژه  یا یک پکیج رو ساختیم  و میخواهیم تو بستر اینترنت بگذاریم تا بقیه هم استفاده کنند 
تو `github` میشه هم پروژه رو مدیریت کرد هم پکیج.
## استفاده خصوصی
ما یک پروژه داریم میتونیم آن را در گیت هاب به صورت خصوصی قرار بدیم تا بعدا اگر پروژه رو حذف کردیم یا از دست دادیم دوباره آن را داشته باشیم .
### و...

## جستجو در `git hub`
## روش اول 
در سایت `git hub` کلمه `laravel` رو جستجو میکنیم  
## روش دوم
در گوگل کلمه `laravel   github` رو جستجو میکنیم .

بعد از جستجو پکیج `laravel`  آن را `clone` کرده یا دانلود می کنیم.  
بعد ان  را استخراج کنیم نام پوشه میشه نام پروژه لاراول ما حالا در اینجا در خط فرمان به پوشه مورد نظر رفته و دستورات زیر رو میزنیم:
```php
composer install
```
وابستگی های مورد نیاز رو نصب میکند حالا اگر به فایل `env.` رفته و اطلاعات دیتابیس رو وارد میکنیم بعد 
```php
php artisan:migrate
```
تا آن رو ایجاد کند حالا میتونیم ازش استفاده کنیم.

---
## یادآوری روش های ایجاد پروژه لاراول
### 1- کلون کردن ریپازیتوری لاراول از GitHub
```bash
git clone https://github.com/laravel/laravel.git
```
### 2- ساخت پروژه با Composer
```php
composer create-project laravel/laravel myproject
```
### 3- نصب Laravel Installer و بعد
```php
laravel new myproject
```
## سوال مهم
من با هر 3 روش بالا پروژه لاراول ایجاد کردم اما چرا اونی که از `github` اومدم `clone` کردم حجمش نسبت به بقیه کم تر هست؟

✅ در لاراول تمام پکیج‌ها (چه خود لاراول، چه بقیه) **داخل پوشه `vendor/` هستند** وقتی با روش `git clone` میخواهیم پروژه لاراول ایجاد کنیم این پوشه `vendor` وجود ندارد به خاطر همین حجم پوشه کم هست.
## راه حل
باید بعد از دریافت پروژه در خط فرمان به پوشه مورد نظر رفته و `composer install` رو بزنیم تا `vendor` را ایجاد کند.
در واقع :
- فایل `composer.json` = لیست خرید 🛒
- پوشه `vendor/` = اجناس خریداری‌شده 📦

وقتی بزنی:

`composer install`

✔️ همه چی می‌ریزه داخل `vendor/` یعنی لیست خرید رو میره میخره و داخل اجناس خریداری شده قرار میده.

## توضیح دقیق `composer install`
- اگر **`composer.lock` وجود نداشته باشد**  
    → دستور Composer لیست پکیج‌ها را از **`composer.json`** می‌خواند  
    → **جدیدترین نسخه‌های سازگار** را نصب می‌کند  
    → و در آخر **`composer.lock` می‌سازد**
    
- اگر **`composer.lock` وجود داشته باشد**  
    → دستور Composer **نسخه‌های دقیق داخل `composer.lock`** را نصب می‌کند  
    → نه نسخه‌های جدید  
    → و `composer.json` فقط نقش مرجع را دارد
    

📦 همهٔ پکیج‌ها در پوشهٔ **`vendor/`** نصب می‌شوند.

---
# جلسه دوم
## روش های دریافت پروژه از `git hub`
در سایت `git hub` بر روی پروژه مورد نظر رفته در قسمت `code` یک لینک پروژه برای `clone` وجود دارد و یک دکمه `download zip` که پروژه رو دانلود کنیم.
اگر دانلود شود پوشه `.git` پروژه وجود ندارد ولی اگر `clone` شود `.git` وجود دارد :
```bash
git clone https://github.com/Mo7ammad73/resume.git
```
---
# جلسه سوم
در این ویدیو یک حساب کاربری در git hub ساخته شد.

---
## جلسه چهارم
ایجاد `repository` در سایت `git hub`
بعد از ایجاد `repository` توضیح زیر رو داریم :
## ایجاد مخزن `Repository` جدید از طریق خط فرمان
```bash
echo "# ali" >> README.md   ایجاد فایل
git init                ایجاد گیت
git add README.md           اضافه کردن فایل به ناحیه استیج
git commit -m "first commit"     ایجاد کامیت 
git branch -M main     تغییر نام شاخه به main
git remote add origin https://github.com/Mo7ammad73/ali.git
git push -u origin main
```
## یا یک مخزن موجود را از خط فرمان وارد`push` کنید:
```bash
git remote add origin https://github.com/Mo7ammad73/ali.git
git branch -M main
git push -u origin main
```

## جلسه پنجم
## دستور `git remote`
برای اینکه با مخزن `repository` راه دور ارتباط برقرار کنیم باید آدرسش رو در گیت تعریف کنیم که برای اینکار از `remote` استفاده میکنیم .
```bash
git remote     add        remote-name   url github
git remote     add        origin                    https://github.com/username/myproject.git
```
## ۱️⃣ وقتی پروژه از GitHub می‌گیری (`git clone`)

- دستور:
```bash
git clone https://github.com/user/project.git
```
- گیت **Git خودش remote با اسم `origin` می‌سازه**
- آدرس `origin` = همون URL که باهاش clone کردی
- حالا می‌تونی مستقیم:
```bash
git pull origin main
git push origin main
```
رو انجام بدی
- **نیازی نیست دستی remote اضافه کنی**

---
## ۲️⃣ وقتی پروژه لوکال داری و می‌خوای به GitHub وصلش کنی

- مراحل:
1. تو GitHub یک **ریپازیتوری خالی** بساز
2. لوکال:

```bash
git remote add origin https://github.com/user/project.git
```
بعد push می‌کنی:
```bash
git push -u origin main
```
✅ حالا remote `origin` تعریف شده و می‌تونی pull و push انجام بدی.
## نمایش `remote`ها
```bash
git remote
```
دستور بالا فقط نام `remote` رو برمیگرداند برای اینکه علاوه بر نام آدرس هم نمایش داده شود:
```bash
git remote    -v
```

---
## جلسه ششم
## دستور `git push`
```bash
git push    remote-name             branch-name
```
ابتدا با دستور زیر نام شاخه اصلی رو از `master` به `main` تغییر میدهیم :
```bash
git branch -m    main
```
حالا میریم برای اینکه پروژه لوکال رو بفرستیم به ریپازیتوری خالی که تو `git hub` ساختیم :
```bash
git push   origin     main  
```
اگر بار اول باشه یک صفحه باز میشه که رمز عبور گیتهاب رو میپرسه بعد دیگه کار میکند.
## نکته
اگر یک `branch` یا شاخه دیگه داشته باشیم بعد داخلش کامیت بزنیم و بعد بخواهیم این کامیتها در گیتهاب نمایش داده شوند باید شاخه مورد نظر رو با دستور زیر به گیتهاب بفرستیم :
```bash
git push origin      new-branch
```
---
## جلسه هفتم 
در این ویدیو در مورد فایل `README.MD` صحبت شد این فایل که مارک داون `mark down`  هست رو ایجاد کرده و درونش متن مورد نیاز رو مینویسیم بعد :
```bash
git add    README.MD
git commit -m "add readme.md  file"
git push origin main
```


---

# 📚 جزوه کامل: ایجاد مخزن (Repository) در GitHub و کار با دستورات Git

---

## 🔹 فصل ۱: مفاهیم پایه

### تفاوت **Git** و **GitHub**

| مفهوم      | توضیح                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| **Git**    | سیستم کنترل نسخه‌ی توزیع‌شده (Local) که روی کامپیوتر شما نصب می‌شود             |
| **GitHub** | پلتفرم آنلاین برای هاست کردن مخازن Git و همکاری تیمی (مثل "ابر" برای کدهای شما) |

### مخزن (Repository) چیست؟

- پوشه‌ای که توسط Git مدیریت می‌شود
- تمام تغییرات فایل‌ها در آن ضبط و ذخیره می‌شود
- دو نوع دارد:
    - **مخزن محلی (Local)**: روی کامپیوتر شما
    - **مخزن راه‌دور (Remote)**: روی سرورهای GitHub

---

## 🔹 فصل ۲: ایجاد مخزن در GitHub (از طریق وب‌سایت)

### مراحل ایجاد مخزن جدید:

1. وارد [github.com](https://github.com/) شوید
2. روی علامت **+** در گوشه بالا سمت راست کلیک کنید → **New repository**
3. فیلدها را پر کنید:
    - **Repository name**: نام مخزن (مثلاً `ali`)
    - **Description**: توضیحات (اختیاری)
    - **Visibility**:
        - `Public` → رایگان و برای همه قابل مشاهده
        - `Private` → فقط برای شما و افراد مجاز
4. ✅ تیک **"Add a README file"** را بزنید (توصیه می‌شود)
5. روی دکمه **Create repository** کلیک کنید

> 💡 نکته: اگر تیک README را نزنید، بعداً باید از طریق خط فرمان مخزن را راه‌اندازی کنید (همانطور که در ادامه توضیح داده شده).

---

## 🔹 فصل ۳: راه‌اندازی مخزن جدید از طریق خط فرمان (Command Line)

### سناریو: شما یک پوشه جدید روی کامپیوتر دارید و می‌خواهید آن را به GitHub متصل کنید

```bash
# مرحله ۱: ایجاد فایل توضیحات پروژه
echo "# ali" >> README.md

# مرحله ۲: تبدیل پوشه به مخزن Git
git init

# مرحله ۳: اضافه کردن فایل‌ها به ناحیه استیج (آماده‌سازی برای کامیت)
git add README.md
# یا برای اضافه کردن همه فایل‌ها:
# git add .

# مرحله ۴: ذخیره تغییرات با پیام توضیحی
git commit -m "first commit"

# مرحله ۵: تغییر نام شاخه پیش‌فرض از 'master' به 'main' (استاندارد جدید)
git branch -M main

# مرحله ۶: اتصال مخزن محلی به مخزن راه‌دور در GitHub
git remote add origin https://github.com/Mo7ammad73/ali.git

# مرحله ۷: ارسال تغییرات به سرور GitHub
git push -u origin main
```

### 🔍 توضیح دقیق هر دستور:

| دستور                         | عملکرد                                          | نکته مهم                                                                            |
| ----------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------- |
| `echo "# ali" >> README.md`   | ایجاد فایل `README.md` با محتوای `# ali`        | `>>` محتوا را به انتهای فایل اضافه می‌کند (اگر فایل وجود نداشته باشد، ایجاد می‌شود) |
| `git init`                    | تبدیل پوشه فعلی به مخزن Git محلی                | پوشه‌ی مخفی `.git` ایجاد می‌شود                                                     |
| `git add README.md`           | انتقال فایل به "ناحیه استیج" (آماده برای کامیت) | بدون این مرحله، فایل‌ها کامیت نمی‌شوند                                              |
| `git commit -m "..."`         | ذخیره‌ی دائمی تغییرات با پیام توضیحی            | `-m` مخفف "message" است                                                             |
| `git branch -M main`          | تغییر نام شاخه پیش‌فرض به `main`                | `-M` = Move + Force (تغییر نام اجباری)                                              |
| `git remote add origin [URL]` | ایجاد ارتباط با مخزن راه‌دور                    | `origin` نام مستعار (alias) برای آدرس راه‌دور است                                   |
| `git push -u origin main`     | ارسال شاخه `main` به `origin`                   | `-u` اتصال شاخه‌های محلی و راه‌دور را برای دفعات بعدی ذخیره می‌کند                  |

> ⚠️ **هشدار امنیتی**: آدرس `https://github.com/...` حاوی اطلاعات عمومی است، اما اگر از `SSH` استفاده کنید (مثل `git@github.com:...`) امنیت بیشتری دارید.

---

## 🔹 فصل ۴: اتصال پروژه‌ی موجود به مخزن جدید در GitHub

### سناریو: شما از قبل یک پروژه با تاریخچه‌ی Git دارید و می‌خواهید آن را به مخزن جدید در GitHub متصل کنید

```bash
# مرحله ۱: اتصال به مخزن راه‌دور (بدون نیاز به `git init` چون مخزن از قبل وجود دارد)
git remote add origin https://github.com/Mo7ammad73/ali.git

# مرحله ۲: مطمئن شوید شاخه اصلی شما `main` نام دارد
git branch -M main

# مرحله ۳: ارسال تمام تاریخچه به GitHub
git push -u origin main
```
### 🔍 تفاوت با سناریوی قبلی:

- نیازی به `git init` نیست چون پوشه از قبل یک مخزن Git است (پوشه‌ی `.git` وجود دارد)
- ممکن است نیاز باشد قبل از `push`، تغییرات را با دستور `git pull --rebase origin main` همگام‌سازی کنید (اگر مخزن راه‌دور خالی نباشد)

---

## 🔹 فصل ۵: تنظیمات اولیه ضروری قبل از شروع کار

این تنظیمات را **یک‌بار** در سیستم خود انجام دهید:

```bash
# تنظیم نام کاربری (برای نمایش در تاریخچه کامیت‌ها)
git config --global user.name "Your Name"

# تنظیم ایمیل (باید همان ایمیلی باشد که در حساب GitHub استفاده می‌کنید)
git config --global user.email "your.email@example.com"

# تنظیم ویرایشگر پیش‌فرض برای پیام‌های کامیت
git config --global core.editor "code --wait"  # برای VS Code
```

> ✅ بررسی تنظیمات فعلی:
```bash
git config --list
```

---

## 🔹 فصل ۶: نکات حرفه‌ای و عیب‌یابی

### 🔸 اگر خطای `fatal: remote origin already exists` دیدید:
```bash
# ابتدا مسیر فعلی را حذف کنید:
git remote remove origin

# سپس دوباره اضافه کنید:
git remote add origin https://github.com/Mo7ammad73/ali.git
```

### 🔸 اگر خطای `rejected` در هنگام `push` دیدید:

```bash
# ابتدا تغییرات راه‌دور را دریافت کنید:
git pull --rebase origin main

# سپس دوباره ارسال کنید:
git push origin main
```

### 🔸 فایل `.gitignore` چیست؟

- فایلی برای مشخص کردن فایل‌هایی که **نباید** در مخزن ذخیره شوند
- مثال محتوای `.gitignore`:
```bash
# فایل‌های موقتی پایتون
__pycache__/
*.pyc

# فایل‌های محیط مجازی
venv/
.env

# فایل‌های سیستم عامل
.DS_Store
Thumbs.db
```

### 🔸 دستورات کاربردی روزمره:
```bash
git status        # وضعیت فعلی مخزن (فایل‌های تغییر کرده، استیج شده و ...)
git log           # مشاهده تاریخچه کامیت‌ها
git diff          # مشاهده تفاوت‌های فایل‌های تغییر کرده
git restore .     # لغو تغییرات محلی (قبل از استیج)
git reset HEAD .  # خارج کردن فایل‌ها از ناحیه استیج
```
---

## 🔹 فصل ۷: خلاصه‌ی فرآیند کامل (چک‌لیست)

✅ **برای مخزن جدید:**

- ایجاد مخزن در GitHub (با یا بدون README)
- تنظیم `user.name` و `user.email`
- `git init` در پوشه محلی
- ایجاد/ویرایش فایل‌ها
- `git add .`
- `git commit -m "پیام معنادار"`
- `git branch -M main`
- `git remote add origin [URL]`
- `git push -u origin main`

✅ **برای پروژه موجود:**

- مطمئن شوید پوشه شامل `.git` است
- `git remote add origin [URL]`
- `git branch -M main` (در صورت نیاز)
- `git push -u origin main`

---

## 📌 نکته پایانی

> همیشه قبل از `push`، با `git pull` تغییرات راه‌دور را دریافت کنید تا تداخل (conflict) ایجاد نشود. این کار به‌ویژه در پروژه‌های تیمی **ضروری** است.

---

✅ این جزوه تمام مراحل از صفر تا صد ایجاد و اتصال مخزن به GitHub را پوشش می‌دهد. برای یادگیری عمیق‌تر، توصیه می‌کنیم دستورات را عملی امتحان کنید و خروجی هر دستور را با `git status` بررسی نمایید.